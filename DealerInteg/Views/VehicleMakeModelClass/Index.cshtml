@model IEnumerable<DealerPortalCRM.ViewModels.VehicleMakeModelClassViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table table-responsive">
        <tr>

            <th>
                VehicleMakeTypeDisplayName
            </th>
            <th>
                VehicleModelTypeDisplayName
            </th>
            <th>
                VehicleClassTypeDisplayName
            </th>
            <th>
                VehicleMakeModelClassCreatedDate
            </th>
            <th>
                VehicleMakeModelClassModifiedDate
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.VehicleMakeTypeDisplayName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VehicleModelTypeDisplayName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VehicleClassTypeDisplayName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VehicleMakeModelClassCreatedDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VehicleMakeModelClassModifiedDate)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new {id = item.VehicleMakeModelClassID}) |
                    @Html.ActionLink("Details", "Details", new {id = item.VehicleMakeModelClassID}) |
                    @Html.ActionLink("Delete", "Delete", new {id = item.VehicleMakeModelClassID})
                </td>
            </tr>
        }

    </table>
