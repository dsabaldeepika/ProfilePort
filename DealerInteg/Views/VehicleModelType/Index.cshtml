@model IEnumerable<DealerPortalCRM.ViewModels.VehicleModelTypeViewModel>
@{
    ViewBag.Title = "Index";
}

<h2>Vehicle Model</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Vehicle Make
        </th>
        <th>
            Model Name
        </th>
        <th>
           Modified By
        </th>
        <th>
           Modified Date
        </th>
        <th></th>
       </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleMakeTypeDisplayName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleModelTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleModelTypeModifiedByID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleModelTypeModifiedDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VehiclModelTypeID }) 
        </td>
    </tr>
}

</table>
